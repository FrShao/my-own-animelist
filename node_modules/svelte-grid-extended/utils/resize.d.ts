import type { ActionReturn } from 'svelte/action';
import type { ItemSize } from '../types';
declare type ResizeOptions = {
    min?: ItemSize;
    max?: ItemSize;
    resizerClass?: string;
    bounds?: boolean;
};
declare type ResizeAtributes = {
    'on:resizestart'?: ResizeEventHandler;
    'on:resizing'?: ResizeEventHandler;
    'on:resizeend'?: ResizeEventHandler;
};
declare type ResizeEventHandler = (e: CustomEvent<ResizeEvent>) => void;
export declare type ResizeEvent = {
    width: number;
    height: number;
};
export default function resize(node: HTMLElement, options?: ResizeOptions): ActionReturn<ResizeOptions, ResizeAtributes>;
export {};
