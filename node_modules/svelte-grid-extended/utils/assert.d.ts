import type { GridSize, ItemSize } from '../types';
export declare type GridOptions = {
    cols: GridSize;
    rows: GridSize;
    itemSize: Partial<ItemSize>;
};
export declare function assertGridOptions(options: GridOptions): void;
