import type { ActionReturn } from 'svelte/action';
declare type MoveOptions = {
    position?: {
        left: number;
        top: number;
    };
    bounds?: boolean;
};
declare type MoveAtributes = {
    'on:movestart'?: MoveEventHandler;
    'on:moving'?: MoveEventHandler;
    'on:moveend'?: MoveEventHandler;
};
declare type MoveEventHandler = (e: CustomEvent<MoveEvent>) => void;
export declare type MoveEvent = {
    left: number;
    top: number;
};
export default function move(node: HTMLElement, options?: MoveOptions): ActionReturn<MoveOptions, MoveAtributes>;
export {};
