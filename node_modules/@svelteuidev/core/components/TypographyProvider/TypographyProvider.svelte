<script>import useStyles from './TypographyProvider.styles';
import { Box } from '../Box';
export let use = [], element = undefined, className = '', override = {}, primaryColor = 'blue', underline = true;
export { className as class };
$: ({ cx, getStyles } = useStyles({ primaryColor, underline }));
</script>

<Box bind:element class={cx(className, getStyles({ css: override }))} {use} {...$$restProps}>
	<slot />
</Box>
