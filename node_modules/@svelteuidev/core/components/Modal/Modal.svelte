<script>import useStyles from './Modal.styles';
import { CloseButton } from '../ActionIcon';
import { Text } from '../Text';
import { Paper } from '../Paper';
import { Overlay } from '../Overlay';
import { OptionalPortal } from '../Portal';
import { Box } from '../Box';
import { randomID, colorScheme, css } from '../../styles';
import { focustrap, lockscroll } from '@svelteuidev/composables';
import { createEventDispatcher, onMount } from 'svelte';
import { fade, scale } from 'svelte/transition';
import { sineInOut } from 'svelte/easing';
export let use = [], element = undefined, className = '', override = {}, opened = false, title = '', zIndex = 200, overflow = 'outside', withCloseButton = true, overlayOpacity = null, overlayColor = null, overlayBlur = 0, radius = 'sm', size = 'md', closeButtonLabel = 'svelteui-close-button', id = 'svelteui', shadow = 'lg', padding = 'lg', closeOnClickOutside = true, closeOnEscape = true, trapFocus = false, centered = null, target = '#SVELTEUI_PROVIDER', withinPortal = true;
export { className as class };
const dispatch = createEventDispatcher();
const castAny = (self) => self;
const baseId = randomID(id);
const titleId = `${baseId}-title`;
const bodyId = `${baseId}-body`;
const _overlayOpacity = typeof overlayOpacity === 'number' ? overlayOpacity : $colorScheme === 'dark' ? 0.85 : 0.75;
const closeOnEscapePress = (event) => {
    if (!trapFocus && event.code === 'Escape' && closeOnEscape) {
        onClose();
    }
};
function onClose() {
    dispatch('close');
}
// Temporary, just add zIndex to Portal component
const zIndexStyles = css({ zIndex });
$: {
    onMount(() => {
        if (!trapFocus) {
            typeof window !== 'undefined'
                ? window.addEventListener('keydown', closeOnEscapePress)
                : null;
        }
    });
}
$: if (opened && ((typeof target === 'string' && !document.querySelector(target)) || !target)) {
    throw new Error('Wrap your app in the SvelteUIProvider, or provide a sufficent target throught the "target={\'\'}" prop ');
}
$: lockScroll = opened;
$: ({ cx, classes, getStyles } = useStyles({ centered, overflow, size, zIndex }));
</script>

{#if opened}
	<OptionalPortal class={zIndexStyles()} {withinPortal} {target}>
		<Box
			id={baseId}
			bind:element
			{use}
			{...$$restProps}
			class={cx(className, getStyles({ css: override }))}
		>
			<div
        role="presentation"
				class={classes.inner}
				use:lockscroll={lockScroll}
				on:keydown|capture={(event) => {
					const shouldTrigger =
						castAny(event.target)?.getAttribute('data-svelteui-stop-propagation') !== 'true';
					shouldTrigger && event.code === 'Escape' && closeOnEscape && onClose();
				}}
			>
				<div transition:scale={{ duration: 100, easing: sineInOut }}>
					<Paper
						class={classes.modal}
						{shadow}
						p={padding}
						{radius}
						role="dialog"
						aria-labelledby={titleId}
						aria-describedby={bodyId}
						aria-modal
						tabIndex={-1}
            use={[focustrap]}
					>
						{#if title || withCloseButton}
							<div class={classes.header}>
								<Text id={titleId} class={classes.title}>
									{title}
								</Text>

								{#if withCloseButton}
									<CloseButton
										iconSize={16}
										on:click={onClose}
										aria-label={closeButtonLabel}
										class={classes.close}
									/>
								{/if}
							</div>
						{/if}
						<div id={bodyId} class={classes.body}>
							<slot>Place some content</slot>
						</div>
					</Paper>
				</div>
				<div transition:fade={{ duration: 200, easing: sineInOut }}>
					<Overlay
						class={classes.overlay}
						override={{ position: 'fixed' }}
						zIndex={0}
						on:mousedown={() => closeOnClickOutside && onClose()}
						blur={overlayBlur}
						color={overlayColor || 'black'}
						opacity={_overlayOpacity}
					/>
				</div>
			</div>
		</Box>
	</OptionalPortal>
{/if}
