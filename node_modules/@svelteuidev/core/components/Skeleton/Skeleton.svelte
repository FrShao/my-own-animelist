<script>import useStyles from './Skeleton.styles';
import { Box } from '../Box';
export let use = [], element = undefined, className = '', override = {}, visible = true, height = 'auto', width = 100, circle = null, radius = null, animate = true;
export { className as class };
$: ({ cx, getStyles } = useStyles({ animate, circle, height, radius, width }));
</script>

<Box
	bind:element
	class={cx(className, { visible }, getStyles({ css: override }))}
	{use}
	{...$$restProps}
>
	<slot />
</Box>
