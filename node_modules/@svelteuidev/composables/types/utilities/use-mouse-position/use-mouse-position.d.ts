import type { Action, Writable } from '../../shared/utilities/types';
export declare type MousePosition = [Writable<{
    x: number;
    y: number;
}>, Action];
export declare function useMousePosition(append?: boolean): MousePosition;
